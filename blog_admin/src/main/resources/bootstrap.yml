spring:
  profiles:
    active: dev
  application:
    name: blog-admin
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml # 文件后缀
