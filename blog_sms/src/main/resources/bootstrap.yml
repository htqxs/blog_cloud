spring:
  profiles:
    active: dev
  application:
    name: blog-sms
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml